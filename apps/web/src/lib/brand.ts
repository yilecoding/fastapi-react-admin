/**
 * 产品标识。改名字、改版本只动这里。
 *
 * 仓库叫 `fastapi-react-admin` —— 描述式命名，为的是被「fastapi react admin」搜到。
 * 代价是同名仓库不止一个（实测 GitHub 上有 5 个），所以**能不能被选**不靠名字，
 * 靠下面这句 tagline 和 README 里那张对比表：细到数据行的权限、真的保状态的多页签、
 * 原生 SQL Server —— 这三样同类里没人同时有。
 *
 * 那枚咬合标记（TenonMark）留着：它现在表示的不是名字，而是这套架构本身 ——
 * `ui ← platform ← web` 严格单向，每层只暴露形状，不靠胶水粘。
 *
 * 命名分两步走，两个字段各自的进度不一样：
 *   - `wordmark`（仓库名/英文标识）还在第一步——描述式命名换搜索可见度，
 *     上面那段就是理由。以后正式改名 "Tenon"，把架构隐喻升格成产品名；
 *     反过来先用 "tenon-admin" 起步不划算，搜索引擎和 GitHub 站内搜都靠
 *     关键词匹配，一个没人听过的名字换不来"fastapi react admin"这条查询的
 *     曝光，等于把第一步的流量白丢了。触发时机不是日期，是「描述式名字
 *     已经跑出可见度」这件事本身发生的时候
 *   - `nameZh`（中文名）已经走完两步，理由是它不背 SEO 的包袱——没有人
 *     会在搜索引擎里敲中文名字来找一个英文技术栈项目，晚换和早换在可见度上
 *     没有任何差别，留着通用词只是白白多等一段时间
 * TenonMark 这枚标记提前把视觉识别铺好，就是为了 `wordmark` 真正改名那天
 * 不用从零建立认知——图形不用换，`nameZh` 也已经先一步对上了。
 */
export const BRAND = {
  /**
   * identity 就是仓库名本身 —— 描述式命名的项目，缩写反而没意义
   * （试过 "FRA"，读起来像机场代码）。小写 + 等宽，看着就是个仓库名。
   */
  wordmark: "fastapi-react-admin",
  /**
   * 产品名，中文。挂在 TenonMark 旁边的就是它（侧边栏顶部、登录页角标、
   * 页脚落款），不是随便一个品类词——"榫卯"直接对应 `wordmark` 以后要改成的
   * "Tenon"，两个语言版本说的是同一件事：那两片咬合的图形。
   */
  nameZh: "榫卯",
  /** 一句话定位，含品类词。站点标题读它，登录页对比表旁边也会引用 */
  tagline: "FastAPI + React 19 中后台底座",
  version: "v0.0.1",
  /** 底座里真正承重的那几样，登录页会把它列出来 */
  stack: ["REACT 19", "TANSTACK", "FASTAPI", "SQL SERVER"],
} as const
