{
  "name": "fastapi-react-admin",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=!@admin/desktop",
    "desktop:dev": "pnpm --filter @admin/desktop dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "i18n:check": "pnpm --filter @admin/i18n check",
    "i18n:fix": "pnpm --filter @admin/i18n check:fix",
    "i18n:jsx": "pnpm --filter @admin/i18n jsx",
    "ctx:check": "node scripts/ctx-check.mjs",
    "brand:icons": "node scripts/gen-brand-icons.mjs",
    "deploy:test": "docker compose -f docker-compose.dev.yml up -d",
    "deploy:test:down": "docker compose -f docker-compose.dev.yml down",
    "deploy:prod:config": "bash deploy/prod.sh config",
    "deploy:prod:build": "bash deploy/prod.sh build",
    "deploy:prod:pull": "bash deploy/prod.sh pull",
    "deploy:prod": "bash deploy/prod.sh up",
    "deploy:prod:down": "bash deploy/prod.sh down",
    "deploy:prod:logs": "bash deploy/prod.sh logs",
    "deploy:prod:status": "bash deploy/prod.sh status",
    "db:upgrade": "pnpm --filter api db:upgrade",
    "db:revision": "pnpm --filter api db:revision",
    "db:current": "pnpm --filter api db:current",
    "db:history": "pnpm --filter api db:history",
    "db:init": "pnpm --filter api db:init",
    "db:reset": "pnpm --filter api db:reset",
    "test": "turbo test",
    "e2e": "turbo e2e",
    "e2e:install": "pnpm --filter web exec playwright install chromium",
    "hooks:install": "apps/api/.venv/bin/prek install -C apps/api -f",
    "install:all": "pnpm install && pnpm --filter api exec uv sync --group dev && pnpm hooks:install && pnpm e2e:install"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "turbo": "^2.10.11",
    "typescript": "~6"
  },
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22.13"
  }
}
